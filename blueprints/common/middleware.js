/*
  It is advised that you do not use the body-parser here. Instead, set up the default in
  your congig.js file, and when needed, override it on the route file. This allows you to
  have sane defaults, and make exceptions when needed, rather than setting it in stone here.
*/

module.exports = [];
